const {
  default: RegisterForm,
} = require('components/RegisterForm/RegisterForm');

const Register = () => {
  return (
    <main>
      <div>
        <RegisterForm />
      </div>
    </main>
  );
};

export default Register;
