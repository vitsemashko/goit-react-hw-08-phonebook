const { default: LoginForm } = require('components/LoginForm/LoginForm');

const Login = () => {
  return (
    <main>
      <div>
        <LoginForm />
      </div>
    </main>
  );
};

export default Login;
